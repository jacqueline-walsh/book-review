<h2><%= @book.title %></h2>
<h3><%= @book.author%></h3>
<p><%= @book.description %></p>

<%= link_to 'edit', edit_book_path(@book), class:"btn btn-link" %> |
<%= link_to 'delete', book_path(@book), class:"btn btn-link", method: :delete, data: {confirm: "Are you sure?"} %>